# OctoPrint Manager

A tool for creating and managing OctoPrint Docker containers.

## Requirements

- Docker installed and running

## Usage

Run the application with:

```bash
./octoprint-manager <config_path>
```

If no config path is provided, it defaults to `./config`
